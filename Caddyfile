# Replace with your actual domain — Caddy provisions and renews the TLS
# certificate for it automatically via Let's Encrypt the first time it starts,
# as long as DNS for this domain already points at this machine's public IP
# and ports 80/443 are forwarded to it.
yourdomain.com {
	reverse_proxy buildverse:3456
}
